<UserControl
    x:Class="ReactiveListTestApp.Views.AddressBookView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:ReactiveListTestApp"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    d:DataContext="{d:DesignInstance Type=local:AddressBookViewModel}"
    d:DesignHeight="450"
    d:DesignWidth="800"
    mc:Ignorable="d">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <!--  Search and Actions  -->
        <StackPanel
            Grid.Row="0"
            Grid.ColumnSpan="2"
            Margin="0,0,0,10"
            Orientation="Horizontal">
            <TextBox
                Width="200"
                Margin="5"
                Text="{Binding SearchQuery, UpdateSourceTrigger=PropertyChanged}" />
            <Button
                x:Name="BulkImportButton"
                Margin="5"
                Content="Import 100 Contacts" />
            <Button
                x:Name="BulkRemoveButton"
                Margin="5"
                Content="Remove HR Inactive" />
        </StackPanel>

        <!--  All Contacts  -->
        <GroupBox
            Grid.Row="1"
            Grid.Column="0"
            Margin="5"
            Header="All Contacts">
            <ListBox
                Height="150"
                DisplayMemberPath="Email"
                ItemsSource="{Binding AllContacts}" />
        </GroupBox>

        <!--  Favorites  -->
        <GroupBox
            Grid.Row="1"
            Grid.Column="1"
            Margin="5"
            Header="Favorites">
            <ListBox
                Height="150"
                DisplayMemberPath="Email"
                ItemsSource="{Binding FavoriteContacts}" />
        </GroupBox>

        <!--  New York Contacts  -->
        <GroupBox
            Grid.Row="2"
            Grid.Column="0"
            Margin="5"
            Header="New York Contacts">
            <ListBox DisplayMemberPath="Email" ItemsSource="{Binding NewYorkContacts}" />
        </GroupBox>

        <!--  Search Results  -->
        <GroupBox
            Grid.Row="2"
            Grid.Column="1"
            Margin="5"
            Header="Search Results">
            <ListBox DisplayMemberPath="Email" ItemsSource="{Binding SearchResults}" />
        </GroupBox>
    </Grid>
</UserControl>
