<UserControl
    x:Class="ReactiveListTestApp.Views.AddressBookView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:ReactiveListTestApp"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    d:DataContext="{d:DesignInstance Type=local:AddressBookViewModel}"
    d:DesignHeight="450"
    d:DesignWidth="800"
    mc:Ignorable="d">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <!--  Search and Actions  -->
        <StackPanel
            Grid.Row="0"
            Grid.ColumnSpan="2"
            Margin="0,0,0,10"
            Orientation="Horizontal">
            <TextBox
                x:Name="SearchTextBox"
                Width="200"
                Margin="5" />
            <Button
                x:Name="BulkImportButton"
                Margin="5"
                Content="Import 100 Contacts" />
            <Button
                x:Name="BulkRemoveButton"
                Margin="5"
                Content="Remove HR Inactive" />
        </StackPanel>

        <!--  All Contacts  -->
        <GroupBox
            Grid.Row="1"
            Grid.Column="0"
            Margin="5"
            Header="All Contacts">
            <ListBox
                x:Name="AllContactsListBox"
                Height="150"
                DisplayMemberPath="Email" />
        </GroupBox>

        <!--  Favorites  -->
        <GroupBox
            Grid.Row="1"
            Grid.Column="1"
            Margin="5"
            Header="Favorites">
            <ListBox
                x:Name="FavoritesListBox"
                Height="150"
                DisplayMemberPath="Email" />
        </GroupBox>

        <!--  New York Contacts  -->
        <GroupBox
            Grid.Row="2"
            Grid.Column="0"
            Margin="5"
            Header="New York Contacts">
            <ListBox x:Name="NewYorkListBox" DisplayMemberPath="Email" />
        </GroupBox>

        <!--  Search Results  -->
        <GroupBox
            Grid.Row="2"
            Grid.Column="1"
            Margin="5"
            Header="Search Results">
            <ListBox x:Name="SearchResultsListBox" DisplayMemberPath="Email" />
        </GroupBox>
    </Grid>
</UserControl>
